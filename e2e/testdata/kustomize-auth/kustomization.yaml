apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
- service.yaml


namespace: example-namespace

secretGenerator:
- name: regcred
  envs:
  - .env.secret

transformers:
- krm-func.yaml
